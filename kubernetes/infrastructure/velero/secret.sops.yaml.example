---
# Velero R2 Credentials Secret
#
# Usage:
#   1. Copy this file: cp secret.sops.yaml.example secret.sops.yaml
#   2. Edit with actual values
#   3. Encrypt: sops -e -i secret.sops.yaml
#   4. Commit the encrypted file
#
# To decrypt for viewing: sops -d secret.sops.yaml
# To edit encrypted file: sops secret.sops.yaml
apiVersion: v1
kind: Secret
metadata:
  name: velero-r2-credentials
  namespace: velero
type: Opaque
stringData:
  cloud: |
    [default]
    aws_access_key_id=REPLACE_WITH_R2_ACCESS_KEY_ID
    aws_secret_access_key=REPLACE_WITH_R2_SECRET_ACCESS_KEY
