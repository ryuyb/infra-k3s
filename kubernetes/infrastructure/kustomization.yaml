---
# Infrastructure root kustomization
# ArgoCD deploys this to manage all infrastructure components
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cert-manager
  - traefik
  - gateway-api
  - velero
  - external-dns
  - prometheus
  - grafana
  # - sealed-secrets  # Optional: uncomment if using sealed-secrets instead of SOPS
