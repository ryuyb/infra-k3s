# Applications configuration
# Domain will be set by ArgoCD Application parameters
domain: DOMAIN_PLACEHOLDER

# pgAdmin4 configuration
pgadmin:
  email: PGADMIN_EMAIL_PLACEHOLDER

# Headlamp configuration
headlamp:
  enabled: true

# Kubewall configuration
kubewall:
  enabled: true

# Authentik configuration
authentik:
  enabled: true

# Redis configuration
redis:
  enabled: false

# Vaultwarden configuration
vaultwarden:
  imageTag: 1.34.3-alpine

# Node selector for stateful workloads (label nodes accordingly)
storageNodeSelector:
  storage-node: "true"

# PostgreSQL application database configuration
# Each application gets its own database, user, and Kubernetes Secret
# Namespace is automatically extracted from helm/apps/templates/{name}.yaml
appDatabases:
  - name: authentik
    database: authentik
    username: authentik
  - name: vaultwarden
    database: vaultwarden
    username: vaultwarden
